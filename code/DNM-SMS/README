'DNM-SMS' Usage
=================
If you are new to DNM and if the data is not large, you only need to add 
the approach of loading data into 'divideDataset.m', and type 'run Main.m';
Moreover, adjusting the value of parameters can further improve 
the performance of model to manage the data.

-Max_Gen: set the number of generation (default 1000)
-popsize: set the population size (default 100)
-M: set the number of dendritic layers (default 8)
-beta: set movement (default [0.9, 0.5, 0.1])
-alpha: set collides (default [0.3, 0.05, 0])
-H: set random threshold (default [0.9, 0.2, 0])
-phases: set the percent of gas phases (default [0.5, 0.1, -0.1]) 
-param: set adjustable param (default [0.85, 0.35, 0.1])

------------------------------- Reference ---------------------------------
Ji, J., Song, S., Tang, Y., Gao, S., Tang, Z., & Todo, Y. (2019). 
Approximate logic neuron model trained by states of matter search algorithm. 
Knowledge-Based Systems, 163, 120-130.
---------------------------------------------------------------------------
